<local:HomePageBase
    x:Class="HyPlayer.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:HomePageViews="using:HyPlayer.Controls.Home"
    xmlns:classes="using:HyPlayer.Classes"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:controls1="using:HyPlayer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:HyPlayer.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    mc:Ignorable="d">
    <local:HomePageBase.Resources>

        <DataTemplate x:Key="HomePagePlaylistTemplate" x:DataType="classes:NCPlayList">
            <Grid
                Padding="16"
                HorizontalAlignment="Stretch"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                ToolTipService.ToolTip="{x:Bind name}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="84" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <controls:ImageEx
                    Width="84"
                    Height="84"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    CornerRadius="8"
                    Source="{x:Bind cover}"
                    Stretch="UniformToFill" />
                <Grid
                    Grid.Column="1"
                    Margin="16,9,0,9"
                    RowSpacing="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock
                        Grid.Row="0"
                        FontSize="14"
                        FontWeight="SemiBold"
                        MaxLines="2"
                        Text="{x:Bind name}"
                        TextTrimming="CharacterEllipsis"
                        TextWrapping="Wrap"
                        ToolTipService.ToolTip="{x:Bind name}" />
                    <TextBlock
                        Grid.Row="1"
                        VerticalAlignment="Bottom"
                        FontSize="14"
                        Foreground="{StaticResource SystemControlForegroundBaseMediumBrush}"
                        MaxLines="1"
                        Text="{x:Bind creater.name, Converter={StaticResource StringListToStringConverter}}"
                        TextTrimming="CharacterEllipsis"
                        ToolTipService.ToolTip="{x:Bind creater.name, Converter={StaticResource StringListToStringConverter}}" />
                </Grid>
            </Grid>
        </DataTemplate>

    </local:HomePageBase.Resources>

    <ScrollViewer x:Name="MainScrollViewer">
        <Grid Margin="0,0,0,128" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="340" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid x:Name="Header" Grid.Row="0">
                <TextBlock
                    x:Uid="HomePage_Title"
                    Margin="40,40,0,0"
                    Canvas.ZIndex="1"
                    FontSize="24"
                    Foreground="White"
                    Style="{StaticResource TitleTextBlockStyle}" />
                <Grid VerticalAlignment="Stretch">
                    <HomePageViews:HomePageHeaderImage
                        x:Name="HeroImage"
                        Height="340"
                        VerticalAlignment="Top" />
                </Grid>
                <StackPanel
                    Margin="40,0,0,0"
                    VerticalAlignment="Center"
                    Spacing="2"
                    Visibility="Visible">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock
                            Margin="0,0,4,0"
                            FontSize="32"
                            FontWeight="Bold"
                            Foreground="White"
                            Style="{StaticResource TitleTextBlockStyle}"
                            Visibility="{x:Bind ViewModel.IsLogined, Mode=OneWay, Converter={StaticResource NegationBoolToVisibilityConverter}}">
                            <Run Text="{x:Bind ViewModel.CurrentUser.name, Mode=OneWay}" />
                            <Run Text="," />
                        </TextBlock>
                        <TextBlock
                            x:Name="Greetings"
                            x:Uid="HomePage_Title"
                            Margin="-4,0,0,0"
                            HorizontalAlignment="Left"
                            FontSize="32"
                            Foreground="White"
                            Style="{StaticResource TitleTextBlockStyle}"
                            Text="{x:Bind ViewModel.Greetings, Mode=OneWay}" />
                    </StackPanel>
                    <TextBlock
                        x:Name="GreetingsText"
                        x:Uid="HomePage_GreetingText"
                        FontSize="16"
                        Foreground="White"
                        Text="{x:Bind ViewModel.GreetingsText, Mode=OneWay}" />
                </StackPanel>
                <StackPanel
                    x:Name="ActionPanel"
                    Margin="0,40,40,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Canvas.ZIndex="1"
                    Orientation="Horizontal"
                    Spacing="12">
                    <Button
                        Width="125"
                        Height="60"
                        Padding="16"
                        CornerRadius="6">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon FontSize="22" Glyph="&#xE006;" />
                            <TextBlock x:Uid="HomePage_Btn_MyLike" FontSize="14" />
                        </StackPanel>
                    </Button>
                    <Button
                        Width="125"
                        Height="60"
                        Padding="16"
                        CornerRadius="6">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon FontSize="22" Glyph="&#xE163;" />
                            <TextBlock x:Uid="HomePage_Btn_DailyRcmd" FontSize="14" />
                        </StackPanel>
                    </Button>
                    <Button
                        Width="125"
                        Height="60"
                        Padding="16"
                        CornerRadius="6">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon FontSize="22" Glyph="&#xEFA9;" />
                            <TextBlock x:Uid="HomePage_Btn_PersonalFm" FontSize="14" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</local:HomePageBase>
