<local:HomePageBase
    x:Class="HyPlayer.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HyPlayer.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:HomePageViews="using:HyPlayer.Controls.Home"
    mc:Ignorable="d">

    <ScrollViewer x:Name="MainScrollViewer">
        <Grid Margin="0,0,0,128" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="340" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid x:Name="Header" Grid.Row="0">
                <TextBlock
                x:Uid="HomePage_Title"
                Margin="40,40,0,0"
                Canvas.ZIndex="1"
                FontSize="24"
                Foreground="White"
                Style="{StaticResource TitleTextBlockStyle}" />
                <Grid VerticalAlignment="Stretch">
                    <HomePageViews:HomePageHeaderImage
                    x:Name="HeroImage"
                    Height="340"
                    VerticalAlignment="Top" />
                </Grid>
                <StackPanel
                Margin="40,0,0,0"
                VerticalAlignment="Center"
                Spacing="2"
                Visibility="Visible">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <TextBlock
                        Margin="0,0,4,0"
                        FontSize="32"
                        FontWeight="Bold"
                        Foreground="White"
                        Style="{StaticResource TitleTextBlockStyle}"
                        >
                        <Run Text="" />
                        <Run Text="," />
                        </TextBlock>
                        <TextBlock
                        x:Name="Greetings"
                        x:Uid="HomePage_Title"
                        Margin="-4,0,0,0"
                        HorizontalAlignment="Left"
                        FontSize="32"
                        Foreground="White"
                        Style="{StaticResource TitleTextBlockStyle}" />
                    </StackPanel>
                    <TextBlock
                    x:Name="GreetingsText"
                    x:Uid="HomePage_GreetingText"
                    FontSize="16"
                    Foreground="White" />
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</local:HomePageBase>
